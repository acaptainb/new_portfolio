# Frontend environment variables
# Local dev can keep this unset and rely on Vite proxy (/api -> localhost:3000).
# For production (GitHub Pages), set this to your deployed backend URL + /api.
# Example: https://your-backend.onrender.com/api
VITE_API_BASE_URL=